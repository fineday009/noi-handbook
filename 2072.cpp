#include <iostream>
using namespace std;

int main()
{
    // 歌手大奖赛上6名评委给一位参赛者打分，6个人打分的平均分为9.6分；如果去掉一个最高分，这名参赛者的平均分为9.4分；如果去掉一个最低分，这名参赛者的平均分为9.    
    
    // (t1 + t2 + t3 + t4 + t5 + t6) / 6 = 9.6
    // (t1 + t2 + t3 + t4 + t5) / 5 = 9.4
    // (t2 + t3 + t4 + t5 + t6) / 5 = 9.8
    // (t2 + t3 + t4 + t5) / 4
    
    double ans = (9.8*5 - 9.6*6 + 9.4*5) / 4;
    printf("%5.2f\n", ans);
    
    return 0;
}